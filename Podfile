platform :ios, '16.0'
use_frameworks!
install! 'cocoapods', :disable_input_output_paths => true

def app_pods
  pod 'Alamofire'
  pod 'ObjectMapper'
  pod 'RealmSwift', '~> 10'
end

target 'todoAppSwiftUI' do
    app_pods
end
